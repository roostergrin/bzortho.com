.pages-treatments
  Loader(v-show='loading')
  div(v-if='props')
    BackgroundLogo
    NavTrigger
      Hero(:props='props.acf.hero')
    Accordion(:props='props.acf.accordion')
    Tabs(:props='props.acf.tabs')
